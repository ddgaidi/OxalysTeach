import type { NextConfig } from "next";

// Configuration globale de Next.js.
// Le projet garde pour l'instant les valeurs par defaut.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
